session: β
root: ~/work
windows:
  - name: v-api-server
    root: cord-backend/projects/api-server
    layout: main-vertical
    commands:
      - source .venv/bin/activate

  - name: api-server
    root: cord-backend/projects/api-server
    layout: main-vertical
    commands:
      - source .venv/bin/activate

  - name: local-dev
    root: cord-backend/local-dev
    commands:
      - docker compose --profile=base up -d liquibase_db_migration liquibase_analytics_migration

  - name: postgres
    root: scratch-pad/sql
    layout: main-vertical
    commands:
      - __load-dbee-postgres

  - name: clickhouse
    root: scratch-pad/sql
    layout: main-vertical
    commands:
      - __load-dbee-clickhouse
