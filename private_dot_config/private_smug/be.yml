session: β
root: ~/work
windows:
  - name: v-api-server
    root: cord-backend/projects/api-server
    layout: main-vertical
    commands:
      - source .venv/bin/activate

  - name: api-server
    root: cord-backend/projects/api-server
    layout: main-vertical
    commands:
      - source .venv/bin/activate

  - name: local-dev
    root: cord-backend/local-dev
    commands:
      - docker compose --profile=base run --rm liquibase_db_migration

  - name: sql
    root: scratch-pad/sql
    layout: main-vertical
    commands:
      - __prepare_postgres_urls
