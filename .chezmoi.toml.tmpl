{{- $work_email := promptString "work_email" -}}
[gpg]
  recipient = "thanhnx.605@gmail.com"
[merge]
  command = "nvim"
  args = "-d"
[git]
  autoCommit = true
  autoPush = true
[data.work]
  email = "{{ $work_email }}"
  git_domain = "{{ $work_git_domain }}"
  gitlinker_matcher = "{{ $work_gitlinker_matcher }}"
