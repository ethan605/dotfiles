# vim:filetype=bash
export EDITOR=vim
export TERM=xterm-256color
export BAT_THEME=base16
export LESS='--LONG-PROMPT --RAW-CONTROL-CHARS --use-color --squeeze-blank-lines --tabs=2'
export MANPAGER='nvim +Man!'

LS_COLORS="$(vivid generate molokai)"
export LS_COLORS

export FZF_DEFAULT_OPTS='
  --ansi
  --bind=ctrl-y:preview-up,ctrl-e:preview-down,ctrl-u:preview-page-up,ctrl-d:preview-page-down
  --color=fg+:7,bg+:0,hl:5,hl+:5
  --color=gutter:0,header:4,marker:2,pointer:5,prompt:2,spinner:3
  --cycle
  --marker="›"
  --pointer="›"
  --prompt="❯ "
  --reverse
  --no-height
'
export FZF_CTRL_R_OPTS='
  --preview="echo {2..} | bat --language=zsh --color=always"
  --bind="ctrl-y:execute-silent(echo -n {2..} | pbcopy)+abort"
  --header="Press CTRL-Y to copy command into clipboard"
'

export PATH="$HOME/.local/bin:$PATH"

# Plugins
eval "$(zoxide init bash)"

# Aliases
alias bat=batcat
alias nano=nvim
alias v=nvim
alias vi=nvim
alias vim=nvim
alias where=which

# Eza
alias ls='eza --group-directories-first --color=always --git --all'
alias l='ls --long --icons=always --header'
alias l1='ls --oneline'
alias lk='l --total-size --sort=size --reverse'
alias lr='l --tree --level=2'

# Shortcuts
alias c=chezmoi
alias k=kubecolor
