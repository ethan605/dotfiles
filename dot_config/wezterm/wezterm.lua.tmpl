-- vim:fileencoding=utf-8:filetype=lua
local wezterm = require("wezterm")

return {
  enable_wayland = true,
  hide_tab_bar_if_only_one_tab = true,

{{- if eq .chezmoi.os "darwin" }}
  font = wezterm.font("OperatorMonoLig Nerd Font"),
  font_size = 18,
{{- else if eq .chezmoi.os "linux" }}
  font = wezterm.font("OperatorMonoLig Nerd Font", { weight = "Light" }),
  font_size = 15,
{{- end }}

  colors = {
    foreground = "#eff0eb",
    background = "#282a36",

    cursor_fg = "#282a36",
    cursor_bg = "#eff0eb",
    cursor_border = "#eff0eb",

    selection_fg = "#282a36",
    selection_bg = "#eff0eb",

    scrollbar_thumb = "#222222",

    ansi    = { "#282a36", "#ff5c57", "#5af78e", "#f3f99d", "#57c7ff", "#ff6ac1", "#9aedfe", "#eff0eb" },
    brights = { "#686868", "#ff5c57", "#5af78e", "#f3f99d", "#57c7ff", "#ff6ac1", "#9aedfe", "#eff0eb" },

    tab_bar = {
      background = "#282a36",

      active_tab = {
        fg_color = "#282a36",
        bg_color = "#eff0eb",
        intensity = "Bold",
      },

      inactive_tab = {
        fg_color = "#eff0eb",
        bg_color = "#282a36",
      },

      inactive_tab_hover = {
        fg_color = "#282a36",
        bg_color = "#686868",
      }
    }
  },
}
